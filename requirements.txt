# Versions pinned with `==` were verified on this machine (Windows 11, Python
# 3.11.9) via a fresh-venv install (pip freeze, exit-0) — FOC-177.

# --- F0: core ML / analysis stack ---
pandas==2.3.3
numpy==2.4.6
scikit-learn==1.7.2
joblib==1.6.0
seaborn==0.13.2
matplotlib==3.11.1
xgboost==2.1.4
scipy==1.17.1
jupyter==1.1.1
jupyterlab==4.6.3
notebook==7.6.2
nbconvert==7.17.1

# --- F0: notebooks + imbalance handling ---
imbalanced-learn==0.14.2
ipykernel==7.3.0
nbformat==5.11.1

# --- F1: tabular deep learning (pinned — last PyPI release is 4.1.0) ---
pytorch-tabnet==4.1.0

# --- F2: stat-context (SCE) from joint-hubs/sce ---
# Available on PyPI (joint-hubs/sce, Apache-2.0); installs clean — version
# verified on this machine:
stat-context==0.4.0

# --- F3: GBDT ensemble + TimesFM (verified on this machine, FOC-175) ---
lightgbm==4.7.0
catboost==1.2.10
# timesfm 3.0.0 is the torch-native backend (no JAX dependency on Windows);
# imports clean alongside torch 2.11.0+cu128:
timesfm==3.0.0

# --- Unpinned: resolve at F4 install time; not yet version-verified ---
plotnine
graphviz
dalex
requests
beautifulsoup4
sentence-transformers
facenet-pytorch

# torch CUDA build — install separately, not from PyPI (2.11.0+cu128 verified,
# CUDA available on RTX 5070 Ti, FOC-175):
# python -m pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128
